[Unit]
# Boot ollama (and let it page the embedding model into RAM) BEFORE raven's
# 22 GB Voron mmap starts pressuring the page cache — otherwise the kernel
# evicts the model and the first AI query pays a multi-minute reload.
# See THE-ARC.md Act XXXII Finding 2 for the original incident.
After=ollama.service
Wants=ollama.service
